
export type OutfitSuggestion = {
  image_url?: string;
  description: string;
};

export type HistoricalFashionItem = {
  year: string;
  image_url?: string;
};

export type StyleTheme = {
  primary: string;
  accent: string;
  background: string;
};
